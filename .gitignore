# Build / dependencies
node_modules/
dist/

# Personal release notes — not for the repo
CHANGELOG.md

# Auto-generated kernel-editor artifacts (regenerated on demand).
# basis/ IS committed: it's a baseline snapshot bundled for kernel-editor
# users who don't have the dashboard app installed (and therefore can't
# generate their own basis from <userData>/sts2-dashboard/).
kernel-editor/kernel-notes/
kernel-editor/node_modules/

# Secret config (Gemini API key)
kernel-editor/llm-config.json

# Editor / OS
.vscode/
*.log
.DS_Store
Thumbs.db
