# Build / install output
node_modules/
dist/

# Stale duplicate of src/main.js at the project root — entry point is src/main.js
# per package.json. Keep this ignored so a leftover copy never gets re-committed.
/main.js

# Editor / OS cruft
.DS_Store
Thumbs.db
desktop.ini
*.log
